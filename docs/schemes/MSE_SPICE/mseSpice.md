# RAMP MSE-SPICE — DPR format & form questions

> Scheme code: `MSE_SPICE`  
> Credit-linked **25% on circular-economy P&M** (cap ₹12.5 L) — brownfield only; second-hand ineligible.

**Related app files:** `MSE_SPICE_STEPS`, `mseSpiceQuestions.ts`, `schemeFormConfig.ts`, `IndividualDPRForm.tsx`

---

## 1. Why this format

Circular-economy **P&M upgrade** pack with sector + existing-unit years on Step 1.

Create New Latest DPR uses its **own 12 consecutive steps**.

---

## 2. Reference

Confirm live RAMP / MSE-SPICE guidelines and circular-sector list before locking subsidy.

---

## 3. Form questions (12 steps)

1. Cover (`circularSector`, `existingUnitYears`, `entrepreneurName`, `proposedPmCost`)  
2–11. Story → … → cost → MoF (grant ≈ 25% of new P&M, cap ₹12.5 L) → operating → viability  
12. Uploads: Udyam, new P&M quotations (new only), bank linkage  

Update this file + `mseSpiceQuestions.ts` + `MSE_SPICE_STEPS` together.
